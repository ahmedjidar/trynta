/**
 * Application shell.
 *
 * UNSTYLED: awaiting handoff app-shell. Structure, routing, providers and the
 * lock screen land in run 2; this is the mount point that makes the scaffold
 * buildable and type-checkable.
 */
export function App() {
  return (
    <main>
      <h1>Keyring</h1>
      <p>Pre-1.0. Not ready for real credentials.</p>
    </main>
  );
}
